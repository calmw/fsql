## 实践

#### 节点格式

- **我们的b树最终将被持久化到磁盘上**。
- 节点包括：
    - 1.一个固定大小的标题，其中包含节点的类型（叶节点或内部节点）和键的数量。
    - 2.指向子节点的指针的列表。（由内部节点使用）。
    - 3.指向每个键值对的集合列表。
    - 4.压缩KV对。

      | type  | nkeys |  pointers  |  offsets   | key-values |
                  |:-----:|:-----:|:----------:|:----------:|:----------:|
      | 2B    |  2B   | nkeys * 8B | nkeys * 2B |    ...     |

- 这是KV对的格式。长度后跟数据

  | klen | vlen |  key  | val  |
      |:----:|:----:|:-----:|:----:|
  |  2B  |  2B  |  ...  | ...  |

- 为了保持简单性，叶节点和内部节点都使用相同的格式。

#### 数据类型

- 我们不能使用内存内的指针，这些指针是引用磁盘页的64位整数，而不是内存内的节点
- 我们还对键和值的大小添加了一些约束条件。所以单KV对的大小总是适合一页。如果您需要支持更大的键或更大的值，您必须为它们分配额外的页面，这就增加了复杂性

#### 解码B树节点
- 


#### 集合列表用于快速定位第KV对
- 